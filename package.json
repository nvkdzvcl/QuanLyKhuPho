{
  "name": "quanlykhupho",
  "private": true,
  "packageManager": "pnpm@10.28.0",
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=10.0.0"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "test:ops": "node --test scripts/test/postgres-operations.test.mjs",
    "db:backup": "node scripts/postgres-backup.mjs",
    "db:restore": "node scripts/postgres-restore.mjs",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.4.4",
    "typescript": "^5.8.2"
  },
  "pnpm": {
    "overrides": {
      "deepmerge-ts@<8.0.0": "8.0.0",
      "exceljs>uuid": "11.1.1"
    }
  }
}
